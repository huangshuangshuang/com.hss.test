package com.haowu.interest;

import java.io.File;

public class Test {
	private static StringBuilder sql=new StringBuilder("insert into user (name,idCard,sex,age,number,address) values(");
    static{
    	File file=new File("C:/Users/300002/Desktop/user.txt");
    }
	public static void main(String[] args) {
        for (int i = 0; i < 10000000; i++) {
        	User user=User.build();
        	sql.append(user.getName()+",");
        	sql.append(user.getIdCard()+",");
        	sql.append(user.getSex()+",");
        	sql.append(user.getAge()+",");
        	sql.append(user.getNumber()+",");
        	sql.append(user.getAddress()+");+\r\n");
		}
        System.out.println("执行完毕");
    }
}
